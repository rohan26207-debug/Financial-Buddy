*.iml
.gradle
/local.properties
/.idea
.DS_Store
/build
/captures
.externalNativeBuild
.cxx
local.properties
app/build/
app/release/
# NOTE: Do NOT ignore app/src/main/assets/ -- it contains the React PWA bundle
# (index.html, static/) that the WebView loads. Ignoring it causes
# net::ERR_FILE_NOT_FOUND when building the APK from a fresh clone.
